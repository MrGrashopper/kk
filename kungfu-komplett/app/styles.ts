import {Lalezar, Permanent_Marker, Saira} from '@next/font/google'

export const permanentMarker = Permanent_Marker({
    weight: '400',
    display: 'swap',
    subsets: ['latin']
})

export const saira = Saira({
    subsets: ['latin'],
    weight: '800',
    display: 'swap'
})

export const lalezar = Lalezar({
    subsets: ['latin'],
    weight: '400',
    display: 'swap'
})
