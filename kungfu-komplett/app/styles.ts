import {Permanent_Marker, Saira} from '@next/font/google'

export const permanentMarker = Permanent_Marker({
    weight: '400',
    display: 'swap',
})

export const saira = Saira({
    weight: '800',
    display: 'swap',
})
